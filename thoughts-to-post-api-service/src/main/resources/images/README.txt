# Generated images are saved here by the Python AI agent for validation.
# Files are named: {platform}_{thoughtId}_{timestamp}.png
# This directory is excluded from version control tracking (see .gitignore).
